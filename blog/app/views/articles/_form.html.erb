<%= form_for(@article) do |f| %>
<div>
<% @article.errors.full_messages.each do |m|%>
<div>
	<h3 class="rojo"><%= m %></h3>
</div>
<%end%>
	<%= f.text_field :title, placeholder: "Titulo"%>
	<br>
	<br>
</div>
<div>
Portada:
<div id="center">
	
	<%= f.file_field :cover %>
	</div>
</div>
<div class="body">
	<%= f.text_area :body, placeholder: "Ingrese Contenido"%>
</div>
<div>
	<%= f.submit "Guardar", class:"btn-lg btn-primary"%>
</div>
<% end %>